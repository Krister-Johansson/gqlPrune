// FIXTURE source file; string-searched and text-scanned only, never compiled.
import { ShapesAnnotatedDoc } from './inline';
import { useQuery } from './stub';

export const Consumer = () => useQuery(ShapesAnnotatedDoc);
